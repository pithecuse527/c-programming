#include <stdio.h>
#include <stdlib.h>

main()
{
  int *arr;
  int size_of_arr = 0;
  int tmp = 0;

  arr = (int*)malloc(sizeof(int));

  do
  {
    *(arr + sizeof_arr) = tmp;
    printf("Type the value %d : ", size_of_arr+1);
    scanf("%d", &tmp);
    size_of_arr++;
    arr = (int*)realloc()


  } while(*arr);


}
